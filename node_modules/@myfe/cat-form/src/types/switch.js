import React from 'react';
import SwitchEditor from '../editors/switch';

export default {

    defaultValue: false,

    renderEditor: function (value, changeHandler) {
        return (
            <SwitchEditor disabled={this.props.disabled}
                          value={value}
                          onChange={changeHandler}/>
        )
    }

}