import React, { Component } from 'react';

class ButtonGroup extends Component {
    render() {
        let className = this.props.vertical ? 'btn-group-vertical' : 'btn-group';
        return (
            <div className={ className }>
                {this.props.children}
            </div>
        )
    }
}

export default ButtonGroup;
