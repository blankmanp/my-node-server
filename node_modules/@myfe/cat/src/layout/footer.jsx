import React from 'react';

let Footer = (props) => {
    return (
        <footer className={`text-muted common-footer ${props.className ? props.className : ''}`}>
            <div className="text-center">
                {props.system.name} ©{new Date().getFullYear()}
            </div>
        </footer>
    )
};

export default Footer;
