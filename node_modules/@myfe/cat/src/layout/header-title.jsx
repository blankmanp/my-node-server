import React, { Component } from 'react';

export default class Title extends Component {
    render() {
        let { breads } = this.props,
            title = breads[breads.length - 1].name;

        return (
            <div className="page-header">
                <h1>{title}</h1>
            </div>
        );
    }
}
