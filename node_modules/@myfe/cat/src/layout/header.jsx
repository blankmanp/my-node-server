import React from 'react';

let Header = (props) => {
    return (
        <header className="navbar navbar-default navbar-static-top navbar-dashboard">
            <div className="container-fluid">
                <div className="navbar-header">
                    <a href="/" className="navbar-brand">
                        <span className="logo-img"></span>
                        {props.system.name}
                    </a>
                </div>
                <nav className="collapse navbar-collapse">
                    { props.renderHeaderContent() }
                    <ul className="nav navbar-nav pull-right">
                        <li>
                            <a href={`${props.user.href ? props.user.href : '/'}`}>
                                <span>{`Hi, ${props.user.name}`}</span>
                            </a>
                        </li>
                        <li>
                            <a href={props.system.logoutUrl ? props.system.logoutUrl : '/logout'}>
                                <i className="glyphicon glyphicon-log-out"></i>
                                <span>退出</span>
                            </a>
                        </li>
                    </ul>
                </nav>
            </div>
        </header>
    )
};

export default Header;
